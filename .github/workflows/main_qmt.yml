
name: Build and deploy Node.js app to Azure Web App - QMT

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Fail fast if package.json is invalid; if invalid, overwrite with a canonical minimal file
      - name: Validate or repair package.json
        shell: bash
        run: |
          if ! jq -e . package.json >/dev/null 2>&1; then
            echo "❌ package.json is not valid JSON. Overwriting with a canonical file."
            cat > package.json <<'JSON'
{
  "name": "spark-template",
  "version": "0.0.0",
  "type": "module",
  "main": "dist/server/index.js",
  "scripts": {
    "start": "node dist/server/index.js",
    "build": "npm run build:client && npm run build:server && npm run copy-client",
    "build:client": "vite build",
    "build:server": "tsc -p tsconfig.server.json",
    "copy-client": "cp -r client/dist/* dist/",
    "verify": "node -e \"JSON.parse(require('fs').readFileSync('package.json','utf8'))\" && echo \"package.json OK\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "mssql": "^9.1.1",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.5",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.2",
    "@types/bcryptjs": "^2.4.2",
    "vite": "^5.0.0"
  },
  "engines": {
    "node": ">=20 <21"
  }
}
JSON
            # Confirm the canonical JSON is valid
            jq -e . package.json >/dev/null 2>&1 || { echo "Canonical package.json still invalid"; exit 1; }
          else
            echo "✅ package.json OK"
          fi

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Install dependencies and clean build
        env:
          NODE_ENV: development
        shell: bash
        run: |
          rm -rf dist
          npm ci --include=dev
          echo "Starting full build..."
          if npm run build; then
            echo "✅ Full build completed"
          else
            echo "⚠️ Full build failed, running fallback (server only)"
            npm install --save-dev @types/node @types/express @types/jsonwebtoken @types/bcryptjs
            npm run build:server
          fi

      - name: Show compiled server entry (first 30 lines)
        run: sed -n '1,30p' dist/server/index.js || echo "⚠️ No server build found"

      - name: Verify compiled code (wildcard route guard)
        shell: bash
        run: |
          if [ -f dist/server/index.js ]; then
            if grep -E "app\\.get\\(\\/.\\*/" dist/server/index.js; then
              echo "✅ Regex wildcard route present"
            elif grep "app.get('/*'" dist/server/index.js; then
              echo "❌ Old string wildcard route found in compiled code"
              exit 1
            else
              echo "ℹ️ No wildcard route found; ensure SPA fallback is configured"
            fi
          else
            echo "⚠️ No server build found, skipping verification"
          fi

      - name: Prepare artifact (dist + runtime manifest)
        shell: bash
        run: |
          test -f dist/index.html || (echo "❌ dist/index.html missing; did copy-client run?" && exit 1)
          echo "Final dist tree (depth 2):"
          find dist -maxdepth 2 -type f | sed 's/^/  /'

      - name: Upload build artifacts (lightweight)
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: |
            dist/
            package.json
            package-lock.json
          # Lighter artifacts: exclude node_modules. Oryx installs production deps on App Service.

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: node-app
          path: .  # flatten so package '.' points at the downloaded content

      - name: Login to Azure (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID }}

      - name: Deploy to Azure Web App (Linux)
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'QMT'
          package: '.'
